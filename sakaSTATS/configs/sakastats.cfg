"sakastats" {
    // @description Min Required ACTIVE Players on Round Start for enabling Stats
    // @value int (0 - maxplayers, default = 3)
    "minRequiredPlayers" "3"
    // @description When a player joins and has played before on the server, send global message
    // @value int (1 = true, 0 = false, default = 1)
    "sendWelcomeBackMsg" "1"
    // @description When a player joins the first time on the server, send global message
    // @value int (1 = true, 0 = false, default = 1)
    "sendFirstWelcomeMsg" "1"
    // @description Count Rocket TopSpeed if Bots are on the server
    // @value int (1 = true, 0 = false, default = 0)
    "countTopSpeedWithBots" "0"
}